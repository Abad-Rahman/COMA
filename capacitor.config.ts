import { CapacitorConfig } from "@capacitor/cli";

const config = {
  appId: "com.chitralabs.orderapp",
  appName: "Chitra Order Manager",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

export default config;
